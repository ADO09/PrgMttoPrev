@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div class="text-center" id="bPdf">
    <button id="idBpdf" type="button" class="btn btn-secondary">Secondary</button>
</div>




<div  class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true" >
    <div class="modal-dialog modal-xl" style="  width:1500px;">
        <div class="modal-content" style="  width:1500px; height:100%; ">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Documento</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <embed id="pdfView" src="" type="application/pdf" width="100%" height="800px">
            </div>
        </div>
    </div>
</div>


<script type="module">
    import { $ } from '/js/utils.js'

    // $('#btnFiltrar').addEventListener('click', () => new bootstrap.Modal($('#exampleModalF')).show());


    $("#bPdf").addEventListener('click', (e) => {
        let tagNameClick = e.target.tagName.toLowerCase();
        let idClick = e.target.id;
        if (idClick == "idBpdf" && tagNameClick === "button") {

            console.log("oaishdoiads");
            // var siguienteEl = e.target.nextElementSibling;
            // var rutaPdf = siguienteEl.innerText;
            var embedElement = document.getElementById('pdfView');
            // var nuevoSrc = host + rutaPdf.trim();

            embedElement.setAttribute('src', '/archivosMttos/PROGRAMACION DE MANTENIMIENTOS MAYO.pdf');

            const modal = new bootstrap.Modal($("#exampleModal"));
            modal.toggle();

        }
    })
</script>